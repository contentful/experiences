# Examples are a local testbed for the SDK packages, not part of the Nx
# project graph. It stays linked via npm workspaces (so it consumes the
# local SDK packages by symlink) but Nx commands like `nx run-many`,
# `nx graph`, `nx affected`, and `nx release` ignore it entirely.
examples
